{/*<TouchableHighlight style={{padding:30, backgroundColor:'lightblue'}}>
          <Text onPress={() => this.props.navigation.navigate('Home', {
            id: 69,
            nombre: "Golosa"
          })}>Ir a noice</Text>
        </TouchableHighlight>
        <TouchableHighlight onPress={this.chin} style={{padding:8, backgroundColor:'lightblue'}}>
          <Text>Poner</Text>
        </TouchableHighlight>
        <TouchableHighlight onPress={this.fetchData} style={{padding:8, backgroundColor:'lightblue'}}>
          <Text>Joas</Text>
        </TouchableHighlight>
        <TouchableHighlight onPress={this.deleteItem} style={{padding:8, backgroundColor:'lightblue'}}>
          <Text>dELETE</Text>
        </TouchableHighlight>

        <ScrollView style={{width:100+"%", borderColor: "red", borderWidth:1}}>
        {
                  this.state.playlist.map((item, index) => (
                     <View key = {item.name} style={{width: '100%', padding:15, backgroundColor: 'green'}} >
                        <Text>{item.name}</Text>
                     </View>
                  ))
               }
              </ScrollView>*/}